//git push -u  origin main --> is used to push code to github
import React from 'react';
import ReactDOM from 'react-dom';
import {App,Test} from "./components/one"

ReactDOM.render(
  <div>
    <App />
    <Test />
  </div>,
  document.getElementById('root')
);


